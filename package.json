{
  "name": "postman-integration-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:postman": "newman run collection.json -e environment.json --reporters cli,junit --reporter-junit-export reports/junit.xml"
  },
  "devDependencies": {
    "newman": "^6.2.0",
    "newman-reporter-junitfull": "^1.1.1"
  }
}
